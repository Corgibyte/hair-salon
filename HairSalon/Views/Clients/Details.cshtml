@model HairSalon.Models.Client

<h2>Client Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)</h3>

<div class="btn-group-vertical">
  @Html.ActionLink("View appointments", "ViewAppts", new { id = Model.ClientId }, new { @role="button", @class="btn" })
  @Html.ActionLink("Edit client", "Edit", new { id = Model.ClientId }, new { @role="button", @class="btn" })
  @Html.ActionLink("Delete client", "Delete", new { id = Model.ClientId }, new { @role="button", @class="btn" })
</div>